package com.banana.bananawhatsapp.persistencia.extended;
/*
import com.banana.bananawhatsapp.modelos.Usuario;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import javax.persistence.TypedQuery;

public class CustomUsuariosRepository implements CustomUsuarioRepository{

    @PersistenceContext
    EntityManager em;
    @Override
    public Usuario get(int idx) {
        TypedQuery q = em.createNamedQuery("Usuario.get", Usuario.class);
        q.setParameter(1, "El nuevo");
        return (Usuario) q.getSingleResult();
    }
}
*/